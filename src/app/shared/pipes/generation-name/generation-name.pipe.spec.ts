import { GenerationNamePipe } from './generation-name.pipe';

describe('GenerationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
